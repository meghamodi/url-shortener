const URL_hash_Mapping = new Map();

function storeURL(clientURL,hash){
    URL_hash_Mapping.set(clientURL,hash)
}

function generateHash(hash){
    return 

}
module.exports={
    storeURL,generateHash
}
